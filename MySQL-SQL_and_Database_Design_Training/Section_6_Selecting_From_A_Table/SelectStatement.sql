USE coffee_store;
-- Get all customers (rows/entries)
SELECT * FROM customers;
-- Get all customer last names
SELECT last_name FROM customers;
-- Get all customer last names with their phone numbers
SELECT last_name, phone_number FROM customers;
